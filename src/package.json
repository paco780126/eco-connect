{
  "name": "jisu-monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "Eco Connect Monorepo. Run 'npm install' then 'npm start'.",
  "workspaces": [
    "client",
    "server",
    "admin"
  ],
  "scripts": {
    "start": "concurrently \"npm start --prefix server\" \"npm run dev --prefix client\" \"npm run dev --prefix admin\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
